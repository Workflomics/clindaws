#!/usr/bin/env bash
# Run clindaws from any working directory.
# Usage: /path/to/clindaws/clindaws-cli <config.json> [options]
SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
PYTHON_BIN="/opt/homebrew/bin/python3"
PYTHONPATH="$SCRIPT_DIR" exec "$PYTHON_BIN" -m clindaws "$@"
